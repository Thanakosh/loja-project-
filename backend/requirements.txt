fastapi==0.110.3
uvicorn[standard]==0.29.0

# ORM + migrações
SQLAlchemy==2.0.30          # já contém o "sqlalchemy" correto
alembic==1.13.1             # ← adicionado

# Postgres driver
psycopg2-binary==2.9.9

# Segurança / env
python-jose[cryptography]==3.3.0
python-dotenv==1.0.1

# Pydantic 1 ou 2 — escolha uma versão; exemplo:
pydantic==1.10.15

WeasyPrint

Jinja2