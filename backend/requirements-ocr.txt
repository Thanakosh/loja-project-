# Dependências opcionais para perfil OCR/ML.
#
# Instalação opcional:
#   pip install -r requirements-ocr.txt
#
# Estas bibliotecas ficam separadas do core para reduzir tempo de build
# e custo operacional do ambiente base da API.

# OCR e processamento de documentos
easyocr>=1.7.1
Pillow>=10.2.0
pdfplumber>=0.10.0
lxml>=5.0.0

# IA generativa (integração futura)
google-generativeai>=0.8.0

# A dependência 'ollama' foi removida do projeto.
